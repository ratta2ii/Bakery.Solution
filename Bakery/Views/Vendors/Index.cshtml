@using Bakery.Models;

<h1>Vendors</h1>

@if (@Model.Count == 0)
{
    <h4>You have no Vendors at this time.</h4>
}

@foreach(Vendor vendor in Model)
{
    <h4><a href="/vendors/@vendor.Id">@vendor.Name</a></h4>
}

<p><a href="/vendors/new">Add a new Vendor</a></p>